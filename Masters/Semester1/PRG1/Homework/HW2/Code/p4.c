#include <stdio.h>

char **create_array(char **arr, ...) {
  char **a;
  return a;
}

char **order(char **arr, ...) {
  char **a;
  return a;
}

int main() {
  char** name = {"Llerenas Gonzalez Pedro David", "Llerenas Gonzalez Arlo Eduardo"};
  FILE* file;
  file = fopen("names.csv", "r");
  create_array(name);
  return 0;
}
