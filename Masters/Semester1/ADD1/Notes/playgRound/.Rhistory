yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
p <- 1000
x<-runif(p, min=-1, max=1)
y<-runif(p, min=-1, max=1)
xc = x / abs(x)
yc = y / abs(y)
dx = x - xc
dy = y - yc
d<-sqrt(dx^2+dy^2)
plot(x,y)
hist(d)
plot(x,y)
hist(d)
plot(x,y)
hist(d)
rug(d)
USArrests
ar = USArrests
View(ar)
mean(ar$Rape)
max(ar$Assault)
ar[max(ar$Assault)]
ar[ar$Assault == max(ar$Assault)]
ar$Assault == max(ar$Assault)
rownames(ar$Assault == max(ar$Assault))
rownames(ar)[ar$Assault == max(ar$Assault)]
rownames(ar)[which.max(ar$Assault)]
