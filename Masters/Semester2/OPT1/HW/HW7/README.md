First, do
`pip install -r requirements.txt`

then, we may run all the programs.

`python p1.py` prints an example of positive and negative definite hessian matrices.
`p2.py` is an implementation of the Newton + Gradient Descent method
`python p4.py` generates 5 plots of a modified BFGS algorithm in its iterations