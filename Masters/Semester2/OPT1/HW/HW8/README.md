# Installing requirements
Run `pip install -r requirements.txt` before running the programs.

# Problem 1
Running `python p1.py` plots the fit polynomials. It also prints to console the MSE for each polynomial.

# Problem 1
Running `python p2.py` plots the fit function obtained from the L-M algorithm. MSE and parameters are printed to screen.