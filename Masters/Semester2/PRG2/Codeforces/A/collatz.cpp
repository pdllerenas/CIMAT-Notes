#include <bits/stdc++.h>

using namespace std;

int solution(int k, int x) {
	for (int i = 0; i < k; i++) {
		if (k & 1) {
			k  w;
		}
	}
}

int main(int argc, char *argv[]) {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  int t;
  cin >> t;

  while (t--) {
    int k, x;
    cin >> k >> x;
    cout << solution(k, x) << endl;
  }
  return 0;
}
