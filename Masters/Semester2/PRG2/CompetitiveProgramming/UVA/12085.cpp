#include <bits/stdc++.h>
using namespace std;

void solve()
{
  int n;
  while (cin >> n && n > 0)
  {
    for (int i = 0; i < n; i++)
    {
      string s;
      cin >> s;

      for ()
    }
  }
}

int main()
{
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  solve();

  return 0;
}