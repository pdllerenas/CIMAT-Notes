#include <string>
#include <iostream>
using namespace std;

void solve()
{
  string D, M, S;
  while ((cin >> D >> M >> S) && D != "E" && M != "N" && S != "D")
  {
    
  }
}

int main()
{
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

  solve();

  return 0;
}