#include <bits/stdc++.h>
using namespace std;

void solution(int n) {
	if (n == 2) {
		cout << 2 << endl;
		return;
	}
	else  {
		cout << n % 2 << endl;
	}
}

int main(int argc, char *argv[]) {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);

	int t;
	cin >> t;
	while (t--) {
		int n;
		cin >> n;
	}

  return 0;
}
